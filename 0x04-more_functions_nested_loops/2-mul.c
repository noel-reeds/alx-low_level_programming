#include "main.h"
/**
 *  mul - performs multiplication on two integers.
 *
 *@a: parameter to multiply.
 *
 *@b: parameter to multiply.
 *
 *Return: Always 0 for success.
 */
int mul(int a, int b)
{
	int res;

	res = a * b;
	return (res);

	return (0);
}
